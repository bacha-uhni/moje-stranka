version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development
      - SERPAPI_KEY=${SERPAPI_KEY}


  test:
    build: .
    command: npm test
    profiles:
      - test

